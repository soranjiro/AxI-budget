{
  "name": "axi-budget",
  "version": "1.0.0",
  "description": "次世代スマート家計簿 PWA",
  "private": true,
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=frontend",
    "build": "npm run build --workspace=frontend",
    "test": "npm run test --workspace=frontend",
    "test:e2e": "npm run test:e2e --workspace=frontend",
    "lint": "npm run lint --workspace=frontend",
    "deploy:dev": "cd infrastructure && terraform workspace select dev && terraform apply -auto-approve && cd ../frontend && npm run build && aws s3 sync dist/ s3://axi-budget-dev --delete",
    "deploy:prod": "cd infrastructure && terraform workspace select prod && terraform apply -auto-approve && cd ../frontend && npm run build && aws s3 sync dist/ s3://axi-budget-prod --delete",
    "schema:generate": "cd api-schema && npm run generate"
  },
  "keywords": [
    "pwa",
    "budget",
    "finance",
    "react",
    "typescript",
    "rust",
    "aws"
  ],
  "author": "AxI Development Team",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.0.0"
  }
}
